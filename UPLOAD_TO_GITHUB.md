# Upload-ready econcompare 0.11.0

This directory is the complete repository root for `noaimastitou/Econcompare` at version 0.11.0.

To update the repository, replace the existing tracked files with the files from this directory while preserving the repository's own hidden `.git` directory.

Important checks before tagging a stable release:

```r
devtools::document()
devtools::test()
devtools::check()
```

The maintainer field in `DESCRIPTION` still contains a placeholder email and should be personalised before a formal registry submission.
